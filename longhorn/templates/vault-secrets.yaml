{{- if and .Values.vault.enabled .Values.vault.secrets }}
{{- range $key, $path := .Values.vault.secrets }}
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "longhorn.fullname" $ }}-{{ $key }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "longhorn.labels" $ | nindent 4 }}
spec:
  vaultAuthRef: {{ include "longhorn.fullname" $ }}-auth
  mount: {{ $.Values.vault.mount }}
  type: kv-v2
  path: {{ $path }}
  refreshAfter: {{ $.Values.vault.refreshAfter }}
  destination:
    name: {{ include "longhorn.fullname" $ }}-{{ $key }}
    create: true
    overwrite: true
{{- end }}
{{- end }}
