{{- if and .Values.vault.enabled .Values.vault.secrets }}
{{- range $key, $path := .Values.vault.secrets }}
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ include "health-checks.fullname" $ }}-{{ $key }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "health-checks.labels" $ | nindent 4 }}
spec:
  vaultAuthRef: {{ include "health-checks.fullname" $ }}-auth
  mount: {{ $.Values.vault.mount }}
  type: kv-v2
  path: {{ $path }}
  refreshAfter: {{ $.Values.vault.refreshAfter }}
  destination:
    name: {{ include "health-checks.fullname" $ }}-{{ $key }}
    create: true
{{- end }}
{{- end }}
