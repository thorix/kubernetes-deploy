apiVersion: v2
name: mcp-gateway
description: MCP Context Forge Gateway - Central MCP hub for AI integrations
type: application
version: 0.1.0
appVersion: "1.0.0-BETA-2"

# The mcp-stack chart is vendored in charts/mcp-stack/
# No external dependencies needed
